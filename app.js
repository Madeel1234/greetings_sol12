// Greetings: start with the array used inexercise 11, but instead of just printing each persons name printing a message to them.
// The text of each message should be the same , but each message should be personlizd with the persons name.
let members = ["Aslam", "Noushad", "Adeel", "Ahmer"];
let message = 'Kal birthday hay kis ki';
for (let i = 0; i < members.length; i++) {
    console.log(message + members[i]);
}
;
export {};
